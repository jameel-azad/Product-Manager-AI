apiVersion: apps/v1
kind: Deployment
metadata:
  name: mee-service
  namespace: platform
  labels:
    app: mee-service
    version: "1.0.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mee-service
  template:
    metadata:
      labels:
        app: mee-service
    spec:
      containers:
        - name: mee-service
          image: xccelera/mee-service:latest
          ports:
            - containerPort: 8008
          envFrom:
            - secretRef:
                name: mee-service-secrets
            - configMapRef:
                name: platform-config
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8008
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8008
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: mee-service
  namespace: platform
spec:
  selector:
    app: mee-service
  ports:
    - port: 8008
      targetPort: 8008
      protocol: TCP
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: mee-service-hpa
  namespace: platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: mee-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
