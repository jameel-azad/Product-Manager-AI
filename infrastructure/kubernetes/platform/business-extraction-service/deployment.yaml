apiVersion: apps/v1
kind: Deployment
metadata:
  name: business-extraction-service
  namespace: platform
  labels:
    app: business-extraction-service
    version: "1.0.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: business-extraction-service
  template:
    metadata:
      labels:
        app: business-extraction-service
    spec:
      containers:
        - name: business-extraction-service
          image: xccelera/business-extraction-service:latest
          ports:
            - containerPort: 8013
          envFrom:
            - secretRef:
                name: business-extraction-service-secrets
            - configMapRef:
                name: platform-config
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8013
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8013
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: business-extraction-service
  namespace: platform
spec:
  selector:
    app: business-extraction-service
  ports:
    - port: 8013
      targetPort: 8013
      protocol: TCP
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: business-extraction-service-hpa
  namespace: platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: business-extraction-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
