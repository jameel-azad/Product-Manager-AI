apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-management-service
  namespace: platform
  labels:
    app: test-management-service
    version: "1.0.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: test-management-service
  template:
    metadata:
      labels:
        app: test-management-service
    spec:
      containers:
        - name: test-management-service
          image: xccelera/test-management-service:latest
          ports:
            - containerPort: 8006
          envFrom:
            - secretRef:
                name: test-management-service-secrets
            - configMapRef:
                name: platform-config
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: test-management-service
  namespace: platform
spec:
  selector:
    app: test-management-service
  ports:
    - port: 8006
      targetPort: 8006
      protocol: TCP
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: test-management-service-hpa
  namespace: platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: test-management-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
