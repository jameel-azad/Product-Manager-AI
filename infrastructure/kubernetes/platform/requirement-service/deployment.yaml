apiVersion: apps/v1
kind: Deployment
metadata:
  name: requirement-service
  namespace: platform
  labels:
    app: requirement-service
    version: "1.0.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: requirement-service
  template:
    metadata:
      labels:
        app: requirement-service
    spec:
      containers:
        - name: requirement-service
          image: xccelera/requirement-service:latest
          ports:
            - containerPort: 8003
          envFrom:
            - secretRef:
                name: requirement-service-secrets
            - configMapRef:
                name: platform-config
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: requirement-service
  namespace: platform
spec:
  selector:
    app: requirement-service
  ports:
    - port: 8003
      targetPort: 8003
      protocol: TCP
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: requirement-service-hpa
  namespace: platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: requirement-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
